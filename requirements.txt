numpy>=1.22,<2.0
pandas>=1.4,<2.2
scikit-learn>=1.1,<1.5
scipy>=1.8
pyarrow>=10.0
matplotlib>=3.5
seaborn>=0.12
pyyaml>=6.0
tqdm>=4.64
shap>=0.41
xgboost>=1.7
joblib>=1.2

# pick one deep-learning backend
tensorflow==2.10.* ; platform_machine != "arm64"
torch==1.13.* ; platform_machine != "arm64"

# arm64 (Apple Silicon) — install manually:
#   pip install tensorflow-macos==2.10 tensorflow-metal
#   or pip install torch==2.0 (CPU build)
